\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{iclr2026/figures/transfer_v13/transfer_map_19.pdf}
    \caption{\textbf{Generation-to-understanding transfer across visual capabilities.}
    Each column corresponds to an I2I task and each row to an understanding capability in \methodname.
    Cells report the change in accuracy, in percentage points, relative to the I2T-only baseline.
    Blue indicates positive transfer and red indicates negative transfer.
    Cells with $p<0.05$ from paired permutation tests against the I2T-only baseline are outlined and labeled with their gains and a star.
    We show capabilities with more than 100 evaluation samples; Appendix~\ref{app:transfer} gives the full matrix and paired significance tests.}
    \label{fig:transfer-map}
\end{figure}

\section{Which generation tasks help which understanding capabilities?}
\label{sec:transfer}
\todo{may need to change the conclusion, add p-test details}
The controlled study shows that I2I training can improve the corresponding I2T task. We now evaluate the 19 I2I tasks in \methodname across its 25 understanding capabilities.

\subsection{Experimental setup}
We follow the I2I $\rightarrow$ I2T recipe: starting from the same BAGEL checkpoint, we use approximately 50k I2I examples followed by 50k LLaVA-Instruct examples~\citep{liu2024llava}. The sources span Recognition (2), Reconstruction (9), and Reorganization (8); Appendix~\ref{app:transfer-setup} specifies the checkpoint set. The baseline is trained only on the I2T data. We evaluate all checkpoints on the same 9,444 evaluation examples and define transfer from source $t$ to capability $c$ as
\[
\Delta(t,c)
=\operatorname{Acc}(M_t,\mathcal D_c)
-\operatorname{Acc}(M_{\mathrm{I2T}},\mathcal D_c),
\]
reported in percentage points. Figure~\ref{fig:transfer-map} shows the 19 capabilities with more than 100 evaluation examples; Appendix~\ref{app:transfer} gives all 25 capabilities and baseline accuracies. Each checkpoint uses a single I2I source, separating the observed effects of different supervision objectives.

\subsection{Transfer depends on the source and target tasks}
\paragraph{Useful source-target pairs can share visual requirements.}
Counting improves with all 19 sources, with 14 gains reaching $p<0.05$. Localization and object pointing yield the largest observed gains ($+3.70$ and $+3.57$ pp, respectively; both $p<0.001$): both objectives require identifying and locating objects. Geometry supervision also benefits metric 3D relation: Z-depth, Euclidean depth, and surface normals improve accuracy by $2.90$, $2.77$, and $2.49$ pp, respectively (all $p<0.03$). These results connect shared visual requirements to useful transfer.

\paragraph{Different sources benefit complementary capabilities.}
Attribute editing improves category recognition ($+2.81$ pp, $p<0.01$), and object editing improves 2D ordering ($+8.42$ pp, $p<0.03$). Inpainting and 2.5D segmentation also improve ordering ($+9.47$ and $+11.58$ pp), while colorization gives the largest observed gain on visual correspondence ($+5.71$ pp; all three $p<0.01$). Useful generation supervision thus extends across recognition, geometry, and spatial reasoning, with different sources benefiting different capabilities.

All 19 checkpoints achieve higher pooled accuracy than the I2T-only baseline. Colorization reaches $70.08\%$, compared with the baseline's $68.90\%$; colorization, 2D edges, and 2D keypoints remain significant after Holm correction across the 19 sources (Appendix~\ref{app:transfer-tests}).

\begin{findingbox}
Image-generation supervision benefits complementary visual capabilities: useful transfer can follow shared visual requirements and cross task-family boundaries.
\end{findingbox}
